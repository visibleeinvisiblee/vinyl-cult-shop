# VINYL CULT — сайт-вітрина

Мінімалістичний односторінковий сайт для виробів Vinyl Cult: товари, кнопка замовлення, посилання на Instagram, ползунки яскравості лампи та швидкості анімації.

---

## Що за що відповідає

| Елемент | Де змінювати | Опис |
|--------|----------------|------|
| **Товари (назва, опис, ціна, фото)** | У файлі `index.html` — блок `PRODUCTS_CONFIG` (масив об'єктів) | Додай/видали об'єкти з полями `title`, `description`, `price`, `image` (шлях до картинки, напр. `images/product1.jpg`). |
| **Посилання для кнопки «Замовити»** | У файлі `index.html` — змінна `ORDER_LINK` | За замовчуванням веде на Instagram; можна замінити на посилання в Telegram, на форму тощо. |
| **Посилання на Instagram** | У файлі `index.html` — змінна `INSTAGRAM_URL` або в розмітці футера | Текст посилання внизу сторінки. |
| **Заголовок і підзаголовок** | У файлі `index.html` — теги `<h1>` та `.subtitle` у секції `header` | Наприклад: «VINYL CULT» і «вироби на замовлення». |
| **Яскравість лампи** | Ползунок «Яскравість лампи» на сторінці | Користувач змінює на сайті; нічого редагувати не потрібно. |
| **Швидкість анімації** | Ползунок «Швидкість анімації» на сторінці | Керує швидкістю обертання вінілового кола; теж лише на сторінці. |
| **Редагування тексту/ціни без коду** | Кнопка «Редагувати контент» внизу справа | Вмикає режим редагування: можна змінювати назву, опис і ціну прямо на сторінці. Після «Зберегти» дані зберігаються в браузері (localStorage). |

---

## Як змінювати товари через код

Відкрий `index.html` і знайди блок:

```javascript
const PRODUCTS_CONFIG = [
  {
    title: 'Назва товару',
    description: 'Опис товару.',
    price: '150 грн',
    image: 'images/product1.jpg'
  },
  // додай ще об'єкти за потреби
];
```

- **title** — назва товару  
- **description** — короткий опис  
- **price** — ціна (текст, можна писати «150 грн» або «від 200 грн»)  
- **image** — шлях до фото щодо папки сайту (фото клади в папку `images/`)

Щоб змінити фото: заміни файли в папці `images/` (наприклад `product1.jpg`, `product2.jpg`) або зміни значення `image` у відповідному об'єкті.

---

## Як змінювати через сторінку (без коду)

1. Відкрий сайт у браузері.  
2. Натисни кнопку **«Редагувати контент»** внизу справа.  
3. Клікай по назві, опису або ціні товару і редагуй текст.  
4. Натисни **«Зберегти»** — зміни збережуться в цьому браузері.  
5. Натисни **«Вийти»**, щоб вимкнути режим редагування.

Фото так змінити не можна — тільки через заміну файлів у папці `images/` та оновлення шляху в `PRODUCTS_CONFIG` (або через майбутнє розширення).

---

## Як запустити локально

Просто відкрий файл `index.html` у браузері або запусти локальний сервер з папки проекту, наприклад:

```bash
# Python 3
python -m http.server 8000

# або npx (Node)
npx serve .
```

Потім відкрий у браузері: `http://localhost:8000`

---

## Як викласти на GitHub (новий репозиторій)

1. **Створи новий репозиторій на GitHub**  
   - Зайди на [github.com](https://github.com) → New repository.  
   - Назва, наприклад: `vinyl-cult-shop`.  
   - Не додавай README, .gitignore чи ліцензію — репозиторій має бути порожнім.

2. **У терміналі в папці проекту виконай:**

```bash
cd d:\COD\vinyl-heart\vinyl-cult-shop
git init
git add .
git commit -m "Vinyl Cult site: products, order button, sliders, Instagram"
git branch -M main
git remote add origin https://github.com/ТВІЙ_ЛОГІН/vinyl-cult-shop.git
git push -u origin main
```

Замість `ТВІЙ_ЛОГІН` підстав свій нік на GitHub. Якщо репозиторій створив з іншою назвою — підстав її замість `vinyl-cult-shop`.

3. **Увімкнути GitHub Pages**  
   - У репозиторії: Settings → Pages → Source: **Deploy from a branch** → гілка **main**, папка **/ (root)** → Save.  
   - Через кілька хвилин сайт буде доступний за посиланням типу:  
   `https://ТВІЙ_ЛОГІН.github.io/vinyl-cult-shop/`

---

## Структура проекту

```
vinyl-cult-shop/
├── index.html    # вся сторінка: розмітка, стилі, скрипти, конфіг товарів
├── images/       # фото товарів (product1.jpg, product2.jpg, …)
└── README.md     # ця інструкція
```

Посилання на Instagram у футері: [@vinyl_cult](https://www.instagram.com/vinyl_cult?igsh=MXd6aXJ5dWRpMDJvNA%3D%3D&utm_source=qr).
